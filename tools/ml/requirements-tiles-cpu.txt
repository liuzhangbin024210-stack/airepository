# Windows / 无 NVIDIA 训练：CPU 版 TensorFlow。
# 推荐：.\tools\ml\install_tiles_venv.ps1  （可加 -Mirror https://mirrors.aliyun.com/pypi/simple/）
# 手动示例（阿里云 + trusted-host，缓解 SSL / 代理环境）:
#   .\.venv-tiles\Scripts\pip install --no-cache-dir --retries 25 --timeout 300 ^
#     -r tools/ml/requirements-tiles-cpu.txt ^
#     -i https://mirrors.aliyun.com/pypi/simple/ ^
#     --trusted-host mirrors.aliyun.com --trusted-host files.pythonhosted.orgtensorflow-cpu>=2.14,<2.19
numpy>=1.24
